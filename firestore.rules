rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Match all documents in the database
    match /{document=**} {
      // Allow read and write access for all users (authenticated and unauthenticated)
      allow read, write: if true;
    }
  }
}

